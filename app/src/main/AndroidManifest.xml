<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FristAPK"
        tools:targetApi="31">
        <activity
            android:name=".ExitCard"
            android:exported="false" />
        <activity
            android:name=".indah"
            android:exported="false" />
        <activity
            android:name=".Kalkulator"
            android:exported="false" />
        <activity
            android:name=".Profile"
            android:exported="false" />
        <activity
            android:name=".KonversiSuhu"
            android:exported="false" /> <!-- Activity untuk menampilkan nota -->
        <activity
            android:name=".Nota"
            android:exported="false" /> <!-- Activity untuk form makanan -->
        <activity
            android:name=".FoodActifity"
            android:exported="false" /> <!-- Activity dengan layout relatif -->
        <activity
            android:name=".RelatifActivity"
            android:exported="true" /> <!-- Activity untuk hasil form -->
        <activity
            android:name=".HasilFormActivity"
            android:exported="false" /> <!-- Activity utama yang dijalankan pertama kali -->
        <activity
            android:name=".Cardactivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>